# ✅ TASK:
# Ask user for a sentence.
# Then:
#  - Print it in uppercase.
#  - Print it reversed.
#  - Print its length.

# 💡 TIP:
# Use `.upper()`, slicing `[::-1]`, and `len()`.
Str=input("Input any string:")
str1=Str.upper()
str2=Str[::-1]
print(str1)
print(str2)
print(len(Str))
