-- Task 8: Character — CONCAT
-- Build a single label column: "Name | Dept | Salary"
-- Hint: CONCAT combines strings. Check the manual for syntax: https://www.s-m-quadri.me/geca/dbms/03
-- What columns do you need to combine? How to separate them?
-- After completing, run 15_check_status.sql to verify.

USE school_db;

-- TODO: SELECT full_name,
--   CONCAT(full_name, ' | ', dept, ' | ', salary) AS label
-- FROM employees;
