-- Task 6: Select with ORDER BY
-- Show students sorted by age (oldest first)

USE school_db;

-- TODO: SELECT all students ORDER BY age DESC
USE school_db;

-- Select all students sorted by age (descending)
SELECT * FROM students
ORDER BY age DESC;